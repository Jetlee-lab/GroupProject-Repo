// import { createReducer } from '@reduxjs/toolkit'

// const counterReducer = createReducer(0, (builder) => {
//     builder
//       .addCase('increment', (state, action) => {...sta})
//       .addCase('decrement', (state, action) => state - action.payload)
//   })
